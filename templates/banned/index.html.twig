{% extends 'base.html.twig' %}

{% block title %}Compte Banni{% endblock %}

{% block content %}
    <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
        <div class="bg-white shadow-lg rounded-lg p-8 text-center">
            <h2 class="text-2xl font-semibold text-red-500">Votre compte est banni.</h2>
            <p class="text-lg text-gray-600 mt-4">
                Vous ne pouvez pas accéder à ces pages. Veuillez contacter un administrateur pour plus d'informations.
            </p>

            {# Afficher les informations de l'utilisateur (si disponibles) #}
            {% if app.user %}
                <p class="text-lg text-gray-800 mt-4">
                    Utilisateur : {{ app.user.firstName }} {{ app.user.lastName }}
                </p>
            {% endif %}

            {# Lien vers la page d'accueil #}
            <div class="mt-6">
                <a href="{{ path('app_home') }}" class="text-orange-600 hover:text-orange-500">Retour à l'accueil</a>
            </div>
        </div>
    </div>
{% endblock %}
