<div class="max-w-xs rounded overflow-hidden shadow-lg bg-white hover:shadow-xl transition-shadow duration-300">
    <img class="w-full h-48 object-cover" src="{{ restaurant.imageUrl ?: 'https://via.placeholder.com/400x300' }}" alt="{{ restaurant.name }}">
    <div class="px-6 py-4">
        <div class="font-bold text-xl text-orange-600">{{ restaurant.name }}</div>
        <p class="text-gray-700 text-base mt-2">{{ restaurant.address }}</p>
        <p class="text-gray-600 text-sm mt-2">Horaires : {{ restaurant.hours }}</p>
    </div>
    <div class="px-6 py-4 flex justify-between items-center">

        <a href="{{ path('restaurant_show', { 'id': restaurant.id }) }}" class="text-white bg-orange-500 hover:bg-orange-600 py-2 px-4 rounded-full">Voir Détails</a>
    </div>
</div>
